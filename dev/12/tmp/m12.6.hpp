// Code generated by Stan version 2.18.0

#include <stan/model/model_header.hpp>

namespace m12_6_model_namespace {

using std::istream;
using std::string;
using std::stringstream;
using std::vector;
using stan::io::dump;
using stan::math::lgamma;
using stan::model::prob_grad;
using namespace stan::math;

static int current_statement_begin__;

stan::io::program_reader prog_reader__() {
    stan::io::program_reader reader;
    reader.add_event(0, 0, "start", "/home/travis/build/StanJulia/StatisticalRethinking.jl/docs/build/12/tmp/m12.6.stan");
    reader.add_event(34, 32, "end", "/home/travis/build/StanJulia/StatisticalRethinking.jl/docs/build/12/tmp/m12.6.stan");
    return reader;
}

class m12_6_model : public prob_grad {
private:
    int N;
    vector<int> T;
    int N_societies;
    vector<int> society;
    vector<int> P;
public:
    m12_6_model(stan::io::var_context& context__,
        std::ostream* pstream__ = 0)
        : prob_grad(0) {
        ctor_body(context__, 0, pstream__);
    }

    m12_6_model(stan::io::var_context& context__,
        unsigned int random_seed__,
        std::ostream* pstream__ = 0)
        : prob_grad(0) {
        ctor_body(context__, random_seed__, pstream__);
    }

    void ctor_body(stan::io::var_context& context__,
                   unsigned int random_seed__,
                   std::ostream* pstream__) {
        typedef double local_scalar_t__;

        boost::ecuyer1988 base_rng__ =
          stan::services::util::create_rng(random_seed__, 0);
        (void) base_rng__;  // suppress unused var warning

        current_statement_begin__ = -1;

        static const char* function__ = "m12_6_model_namespace::m12_6_model";
        (void) function__;  // dummy to suppress unused var warning
        size_t pos__;
        (void) pos__;  // dummy to suppress unused var warning
        std::vector<int> vals_i__;
        std::vector<double> vals_r__;
        local_scalar_t__ DUMMY_VAR__(std::numeric_limits<double>::quiet_NaN());
        (void) DUMMY_VAR__;  // suppress unused var warning

        // initialize member variables
        try {
            current_statement_begin__ = 2;
            context__.validate_dims("data initialization", "N", "int", context__.to_vec());
            N = int(0);
            vals_i__ = context__.vals_i("N");
            pos__ = 0;
            N = vals_i__[pos__++];
            current_statement_begin__ = 3;
            validate_non_negative_index("T", "N", N);
            context__.validate_dims("data initialization", "T", "int", context__.to_vec(N));
            validate_non_negative_index("T", "N", N);
            T = std::vector<int>(N,int(0));
            vals_i__ = context__.vals_i("T");
            pos__ = 0;
            size_t T_limit_0__ = N;
            for (size_t i_0__ = 0; i_0__ < T_limit_0__; ++i_0__) {
                T[i_0__] = vals_i__[pos__++];
            }
            current_statement_begin__ = 4;
            context__.validate_dims("data initialization", "N_societies", "int", context__.to_vec());
            N_societies = int(0);
            vals_i__ = context__.vals_i("N_societies");
            pos__ = 0;
            N_societies = vals_i__[pos__++];
            current_statement_begin__ = 5;
            validate_non_negative_index("society", "N", N);
            context__.validate_dims("data initialization", "society", "int", context__.to_vec(N));
            validate_non_negative_index("society", "N", N);
            society = std::vector<int>(N,int(0));
            vals_i__ = context__.vals_i("society");
            pos__ = 0;
            size_t society_limit_0__ = N;
            for (size_t i_0__ = 0; i_0__ < society_limit_0__; ++i_0__) {
                society[i_0__] = vals_i__[pos__++];
            }
            current_statement_begin__ = 6;
            validate_non_negative_index("P", "N", N);
            context__.validate_dims("data initialization", "P", "int", context__.to_vec(N));
            validate_non_negative_index("P", "N", N);
            P = std::vector<int>(N,int(0));
            vals_i__ = context__.vals_i("P");
            pos__ = 0;
            size_t P_limit_0__ = N;
            for (size_t i_0__ = 0; i_0__ < P_limit_0__; ++i_0__) {
                P[i_0__] = vals_i__[pos__++];
            }

            // validate, data variables
            current_statement_begin__ = 2;
            current_statement_begin__ = 3;
            current_statement_begin__ = 4;
            current_statement_begin__ = 5;
            current_statement_begin__ = 6;
            // initialize data variables


            // validate transformed data

            // validate, set parameter ranges
            num_params_r__ = 0U;
            param_ranges_i__.clear();
            current_statement_begin__ = 9;
            ++num_params_r__;
            current_statement_begin__ = 10;
            validate_non_negative_index("a_society", "N_societies", N_societies);
            num_params_r__ += N_societies;
            current_statement_begin__ = 11;
            ++num_params_r__;
            current_statement_begin__ = 12;
            ++num_params_r__;
        } catch (const std::exception& e) {
            stan::lang::rethrow_located(e, current_statement_begin__, prog_reader__());
            // Next line prevents compiler griping about no return
            throw std::runtime_error("*** IF YOU SEE THIS, PLEASE REPORT A BUG ***");
        }
    }

    ~m12_6_model() { }


    void transform_inits(const stan::io::var_context& context__,
                         std::vector<int>& params_i__,
                         std::vector<double>& params_r__,
                         std::ostream* pstream__) const {
        stan::io::writer<double> writer__(params_r__,params_i__);
        size_t pos__;
        (void) pos__; // dummy call to supress warning
        std::vector<double> vals_r__;
        std::vector<int> vals_i__;

        if (!(context__.contains_r("alpha")))
            throw std::runtime_error("variable alpha missing");
        vals_r__ = context__.vals_r("alpha");
        pos__ = 0U;
        context__.validate_dims("initialization", "alpha", "double", context__.to_vec());
        double alpha(0);
        alpha = vals_r__[pos__++];
        try {
            writer__.scalar_unconstrain(alpha);
        } catch (const std::exception& e) { 
            throw std::runtime_error(std::string("Error transforming variable alpha: ") + e.what());
        }

        if (!(context__.contains_r("a_society")))
            throw std::runtime_error("variable a_society missing");
        vals_r__ = context__.vals_r("a_society");
        pos__ = 0U;
        validate_non_negative_index("a_society", "N_societies", N_societies);
        context__.validate_dims("initialization", "a_society", "vector_d", context__.to_vec(N_societies));
        vector_d a_society(static_cast<Eigen::VectorXd::Index>(N_societies));
        for (int j1__ = 0U; j1__ < N_societies; ++j1__)
            a_society(j1__) = vals_r__[pos__++];
        try {
            writer__.vector_unconstrain(a_society);
        } catch (const std::exception& e) { 
            throw std::runtime_error(std::string("Error transforming variable a_society: ") + e.what());
        }

        if (!(context__.contains_r("bp")))
            throw std::runtime_error("variable bp missing");
        vals_r__ = context__.vals_r("bp");
        pos__ = 0U;
        context__.validate_dims("initialization", "bp", "double", context__.to_vec());
        double bp(0);
        bp = vals_r__[pos__++];
        try {
            writer__.scalar_unconstrain(bp);
        } catch (const std::exception& e) { 
            throw std::runtime_error(std::string("Error transforming variable bp: ") + e.what());
        }

        if (!(context__.contains_r("sigma_society")))
            throw std::runtime_error("variable sigma_society missing");
        vals_r__ = context__.vals_r("sigma_society");
        pos__ = 0U;
        context__.validate_dims("initialization", "sigma_society", "double", context__.to_vec());
        double sigma_society(0);
        sigma_society = vals_r__[pos__++];
        try {
            writer__.scalar_lb_unconstrain(0,sigma_society);
        } catch (const std::exception& e) { 
            throw std::runtime_error(std::string("Error transforming variable sigma_society: ") + e.what());
        }

        params_r__ = writer__.data_r();
        params_i__ = writer__.data_i();
    }

    void transform_inits(const stan::io::var_context& context,
                         Eigen::Matrix<double,Eigen::Dynamic,1>& params_r,
                         std::ostream* pstream__) const {
      std::vector<double> params_r_vec;
      std::vector<int> params_i_vec;
      transform_inits(context, params_i_vec, params_r_vec, pstream__);
      params_r.resize(params_r_vec.size());
      for (int i = 0; i < params_r.size(); ++i)
        params_r(i) = params_r_vec[i];
    }


    template <bool propto__, bool jacobian__, typename T__>
    T__ log_prob(vector<T__>& params_r__,
                 vector<int>& params_i__,
                 std::ostream* pstream__ = 0) const {

        typedef T__ local_scalar_t__;

        local_scalar_t__ DUMMY_VAR__(std::numeric_limits<double>::quiet_NaN());
        (void) DUMMY_VAR__;  // suppress unused var warning

        T__ lp__(0.0);
        stan::math::accumulator<T__> lp_accum__;

        try {
            // model parameters
            stan::io::reader<local_scalar_t__> in__(params_r__,params_i__);

            local_scalar_t__ alpha;
            (void) alpha;  // dummy to suppress unused var warning
            if (jacobian__)
                alpha = in__.scalar_constrain(lp__);
            else
                alpha = in__.scalar_constrain();

            Eigen::Matrix<local_scalar_t__,Eigen::Dynamic,1>  a_society;
            (void) a_society;  // dummy to suppress unused var warning
            if (jacobian__)
                a_society = in__.vector_constrain(N_societies,lp__);
            else
                a_society = in__.vector_constrain(N_societies);

            local_scalar_t__ bp;
            (void) bp;  // dummy to suppress unused var warning
            if (jacobian__)
                bp = in__.scalar_constrain(lp__);
            else
                bp = in__.scalar_constrain();

            local_scalar_t__ sigma_society;
            (void) sigma_society;  // dummy to suppress unused var warning
            if (jacobian__)
                sigma_society = in__.scalar_lb_constrain(0,lp__);
            else
                sigma_society = in__.scalar_lb_constrain(0);


            // transformed parameters



            // validate transformed parameters

            const char* function__ = "validate transformed params";
            (void) function__;  // dummy to suppress unused var warning

            // model body
            {
            current_statement_begin__ = 15;
            validate_non_negative_index("mu", "N", N);
            Eigen::Matrix<local_scalar_t__,Eigen::Dynamic,1>  mu(static_cast<Eigen::VectorXd::Index>(N));
            (void) mu;  // dummy to suppress unused var warning

            stan::math::initialize(mu, DUMMY_VAR__);
            stan::math::fill(mu,DUMMY_VAR__);


            current_statement_begin__ = 16;
            lp_accum__.add(normal_log(alpha,0,10));
            current_statement_begin__ = 17;
            lp_accum__.add(normal_log(bp,0,1));
            current_statement_begin__ = 18;
            lp_accum__.add(cauchy_log(sigma_society,0,1));
            current_statement_begin__ = 19;
            lp_accum__.add(normal_log(a_society,0,sigma_society));
            current_statement_begin__ = 20;
            for (int i = 1; i <= N; ++i) {
                current_statement_begin__ = 20;
                stan::model::assign(mu, 
                            stan::model::cons_list(stan::model::index_uni(i), stan::model::nil_index_list()), 
                            ((alpha + get_base1(a_society,get_base1(society,i,"society",1),"a_society",1)) + (bp * stan::math::log(get_base1(P,i,"P",1)))), 
                            "assigning variable mu");
            }
            current_statement_begin__ = 21;
            lp_accum__.add(poisson_log_log(T,mu));
            }

        } catch (const std::exception& e) {
            stan::lang::rethrow_located(e, current_statement_begin__, prog_reader__());
            // Next line prevents compiler griping about no return
            throw std::runtime_error("*** IF YOU SEE THIS, PLEASE REPORT A BUG ***");
        }

        lp_accum__.add(lp__);
        return lp_accum__.sum();

    } // log_prob()

    template <bool propto, bool jacobian, typename T_>
    T_ log_prob(Eigen::Matrix<T_,Eigen::Dynamic,1>& params_r,
               std::ostream* pstream = 0) const {
      std::vector<T_> vec_params_r;
      vec_params_r.reserve(params_r.size());
      for (int i = 0; i < params_r.size(); ++i)
        vec_params_r.push_back(params_r(i));
      std::vector<int> vec_params_i;
      return log_prob<propto,jacobian,T_>(vec_params_r, vec_params_i, pstream);
    }


    void get_param_names(std::vector<std::string>& names__) const {
        names__.resize(0);
        names__.push_back("alpha");
        names__.push_back("a_society");
        names__.push_back("bp");
        names__.push_back("sigma_society");
        names__.push_back("log_lik");
    }


    void get_dims(std::vector<std::vector<size_t> >& dimss__) const {
        dimss__.resize(0);
        std::vector<size_t> dims__;
        dims__.resize(0);
        dimss__.push_back(dims__);
        dims__.resize(0);
        dims__.push_back(N_societies);
        dimss__.push_back(dims__);
        dims__.resize(0);
        dimss__.push_back(dims__);
        dims__.resize(0);
        dimss__.push_back(dims__);
        dims__.resize(0);
        dims__.push_back(N);
        dimss__.push_back(dims__);
    }

    template <typename RNG>
    void write_array(RNG& base_rng__,
                     std::vector<double>& params_r__,
                     std::vector<int>& params_i__,
                     std::vector<double>& vars__,
                     bool include_tparams__ = true,
                     bool include_gqs__ = true,
                     std::ostream* pstream__ = 0) const {
        typedef double local_scalar_t__;

        vars__.resize(0);
        stan::io::reader<local_scalar_t__> in__(params_r__,params_i__);
        static const char* function__ = "m12_6_model_namespace::write_array";
        (void) function__;  // dummy to suppress unused var warning
        // read-transform, write parameters
        double alpha = in__.scalar_constrain();
        vector_d a_society = in__.vector_constrain(N_societies);
        double bp = in__.scalar_constrain();
        double sigma_society = in__.scalar_lb_constrain(0);
        vars__.push_back(alpha);
            for (int k_0__ = 0; k_0__ < N_societies; ++k_0__) {
            vars__.push_back(a_society[k_0__]);
            }
        vars__.push_back(bp);
        vars__.push_back(sigma_society);

        // declare and define transformed parameters
        double lp__ = 0.0;
        (void) lp__;  // dummy to suppress unused var warning
        stan::math::accumulator<double> lp_accum__;

        local_scalar_t__ DUMMY_VAR__(std::numeric_limits<double>::quiet_NaN());
        (void) DUMMY_VAR__;  // suppress unused var warning

        try {



            // validate transformed parameters

            // write transformed parameters
            if (include_tparams__) {
            }
            if (!include_gqs__) return;
            // declare and define generated quantities
            current_statement_begin__ = 24;
            validate_non_negative_index("log_lik", "N", N);
            Eigen::Matrix<local_scalar_t__,Eigen::Dynamic,1>  log_lik(static_cast<Eigen::VectorXd::Index>(N));
            (void) log_lik;  // dummy to suppress unused var warning

            stan::math::initialize(log_lik, DUMMY_VAR__);
            stan::math::fill(log_lik,DUMMY_VAR__);


            {
            current_statement_begin__ = 26;
            validate_non_negative_index("mu", "N", N);
            Eigen::Matrix<local_scalar_t__,Eigen::Dynamic,1>  mu(static_cast<Eigen::VectorXd::Index>(N));
            (void) mu;  // dummy to suppress unused var warning

            stan::math::initialize(mu, DUMMY_VAR__);
            stan::math::fill(mu,DUMMY_VAR__);


            current_statement_begin__ = 27;
            for (int i = 1; i <= N; ++i) {

                current_statement_begin__ = 28;
                stan::model::assign(mu, 
                            stan::model::cons_list(stan::model::index_uni(i), stan::model::nil_index_list()), 
                            ((alpha + get_base1(a_society,get_base1(society,i,"society",1),"a_society",1)) + (bp * stan::math::log(get_base1(P,i,"P",1)))), 
                            "assigning variable mu");
                current_statement_begin__ = 29;
                stan::model::assign(log_lik, 
                            stan::model::cons_list(stan::model::index_uni(i), stan::model::nil_index_list()), 
                            poisson_log_log(get_base1(T,i,"T",1),get_base1(mu,i,"mu",1)), 
                            "assigning variable log_lik");
            }
            }

            // validate generated quantities
            current_statement_begin__ = 24;

            // write generated quantities
            for (int k_0__ = 0; k_0__ < N; ++k_0__) {
            vars__.push_back(log_lik[k_0__]);
            }

        } catch (const std::exception& e) {
            stan::lang::rethrow_located(e, current_statement_begin__, prog_reader__());
            // Next line prevents compiler griping about no return
            throw std::runtime_error("*** IF YOU SEE THIS, PLEASE REPORT A BUG ***");
        }
    }

    template <typename RNG>
    void write_array(RNG& base_rng,
                     Eigen::Matrix<double,Eigen::Dynamic,1>& params_r,
                     Eigen::Matrix<double,Eigen::Dynamic,1>& vars,
                     bool include_tparams = true,
                     bool include_gqs = true,
                     std::ostream* pstream = 0) const {
      std::vector<double> params_r_vec(params_r.size());
      for (int i = 0; i < params_r.size(); ++i)
        params_r_vec[i] = params_r(i);
      std::vector<double> vars_vec;
      std::vector<int> params_i_vec;
      write_array(base_rng,params_r_vec,params_i_vec,vars_vec,include_tparams,include_gqs,pstream);
      vars.resize(vars_vec.size());
      for (int i = 0; i < vars.size(); ++i)
        vars(i) = vars_vec[i];
    }

    static std::string model_name() {
        return "m12_6_model";
    }


    void constrained_param_names(std::vector<std::string>& param_names__,
                                 bool include_tparams__ = true,
                                 bool include_gqs__ = true) const {
        std::stringstream param_name_stream__;
        param_name_stream__.str(std::string());
        param_name_stream__ << "alpha";
        param_names__.push_back(param_name_stream__.str());
        for (int k_0__ = 1; k_0__ <= N_societies; ++k_0__) {
            param_name_stream__.str(std::string());
            param_name_stream__ << "a_society" << '.' << k_0__;
            param_names__.push_back(param_name_stream__.str());
        }
        param_name_stream__.str(std::string());
        param_name_stream__ << "bp";
        param_names__.push_back(param_name_stream__.str());
        param_name_stream__.str(std::string());
        param_name_stream__ << "sigma_society";
        param_names__.push_back(param_name_stream__.str());

        if (!include_gqs__ && !include_tparams__) return;

        if (include_tparams__) {
        }


        if (!include_gqs__) return;
        for (int k_0__ = 1; k_0__ <= N; ++k_0__) {
            param_name_stream__.str(std::string());
            param_name_stream__ << "log_lik" << '.' << k_0__;
            param_names__.push_back(param_name_stream__.str());
        }
    }


    void unconstrained_param_names(std::vector<std::string>& param_names__,
                                   bool include_tparams__ = true,
                                   bool include_gqs__ = true) const {
        std::stringstream param_name_stream__;
        param_name_stream__.str(std::string());
        param_name_stream__ << "alpha";
        param_names__.push_back(param_name_stream__.str());
        for (int k_0__ = 1; k_0__ <= N_societies; ++k_0__) {
            param_name_stream__.str(std::string());
            param_name_stream__ << "a_society" << '.' << k_0__;
            param_names__.push_back(param_name_stream__.str());
        }
        param_name_stream__.str(std::string());
        param_name_stream__ << "bp";
        param_names__.push_back(param_name_stream__.str());
        param_name_stream__.str(std::string());
        param_name_stream__ << "sigma_society";
        param_names__.push_back(param_name_stream__.str());

        if (!include_gqs__ && !include_tparams__) return;

        if (include_tparams__) {
        }


        if (!include_gqs__) return;
        for (int k_0__ = 1; k_0__ <= N; ++k_0__) {
            param_name_stream__.str(std::string());
            param_name_stream__ << "log_lik" << '.' << k_0__;
            param_names__.push_back(param_name_stream__.str());
        }
    }

}; // model

}

typedef m12_6_model_namespace::m12_6_model stan_model;

